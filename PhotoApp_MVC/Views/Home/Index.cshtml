@model X.PagedList.IPagedList<PhotoPostViewModel>
@{
    ViewData["Title"] = "Home Page";
}

<partial name="_Section" />

<div class="album py-5 bg-body-tertiary">
    <div class="container">
        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
            @foreach (var photoPost in Model)
            {
                <div class="col">
                    <div class="card shadow-sm">
                        <img src="@photoPost.ImageUrl" class="bd-placeholder-img card-img-top" width="100%" height="225" role="img" aria-label="Placeholder: Thumbnail" preserveAspectRatio="xMidYMid slice" focusable="false" alt="@photoPost.Title">
                        <div class="card-body">
                            <h5 class="card-title">@photoPost.Title</h5>
                            <p class="card-text">@photoPost.Description</p>
                            <small class="text-body-secondary">@photoPost.UpdatedAt.ToShortDateString()</small>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <a asp-action="" asp-controller="" asp-route-id="" class="btn btn-sm btn-outline-secondary">@photoPost.CategoryName</a>
                                    <a asp-action="" asp-controller="" asp-route-id="" class="btn btn-sm btn-outline-secondary">@photoPost.UserName</a>
                                    <a asp-action="Details" asp-controller="PhotoPosts" asp-route-id="@photoPost.Id" class="btn btn-sm btn-outline-secondary">Details</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>

        <partial name="_HomePagination" model="@Model" />

    </div>
</div>
